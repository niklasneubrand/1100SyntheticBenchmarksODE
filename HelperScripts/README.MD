This folder contains a multitude of bash scripts that where used to generate the sysnthetic benchmark problemns on our local cluster.
These files are kept for transparency and reproducability, but are not intended to be run on other machines.

During repository clean-up these files were moved to the current folder. To run the scipts correctly they need to be moved back to the main directory of the repository.
