The folder "BaseModels" (my initial naming) contains all template problems.
I did kept the folder name to not break any dependencies.

The templates are split in two groups based on the required recources to perform the simulations. This only had technical reasons for the paralellization of the problem-generation compute jobs.

"OOUTDATED" contains older versions of the templates, where certain modifications were missing and the group split was different.